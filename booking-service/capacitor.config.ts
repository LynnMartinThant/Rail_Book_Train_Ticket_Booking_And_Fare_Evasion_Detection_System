import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mycompany.bookingapp',
  appName: 'MyBookingApp',
  webDir: 'http://localhost:3000/'
};

export default config;
